{{#each valid}}

  <div class="alert alert-danger">{{texto}}</div>
{{else}}

{{/each}}

<div id="card_pesquisaID" class="card" style="display: none">
  <div class="card-body">
    <h2>Procurar produto</h2>
    <form action="/api/produtos" method="GET">
      <label for="id_input">Id do produto: </label>
      <input
        type="text"
        id="id_input"
        name="id"
        placeholder="id do produto"
        class="form-control"
      />
      <button type="submit" class="btn btn-success mt-4">Procurar produto</button>
    </form>
  </div>
</div>